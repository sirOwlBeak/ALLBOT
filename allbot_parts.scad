/* This file is created by sirOwlBeak
 *
 * Bundles modularized .stl import statements so they can be used in other openSCAD scripts
*/

module ServoMount() {
    import("SERVO MOUNT/REMESHED/SERVO MOUNT.stl", convexity=8);
}

ServoMount();